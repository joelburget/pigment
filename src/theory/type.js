// @flow

// import { POKE_HOLE, pokeHole, doPokeHole } from '../commands/pokeHole';


// Do we need a formation rule for Type?
const Type = {
  // TODO
};

export default Type;

// XXX - do we need to find a home for this action/edit pair? We want to be
// able to poke holes in Type. Is it a special case? I guess you can just poke
// a hole in anything by default?
  // actions(): List<Action> {
  //   return List([pokeHole]);
  // }

  // performEdit(id: string): Edit {
  //   invariant(
  //     id === POKE_HOLE,
  //     'Type.edit only knows of POKE_HOLE'
  //   );

  //   return doPokeHole(this);
  // }
